const
    Command = require('./Command'),
    Help = require('./Help'),
    Migrate = require('./Migrate'),
    Query = require('./Query')

module.exports = {
    Command,
    Help,
    Migrate,
    Query,
}